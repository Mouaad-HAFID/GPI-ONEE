import { EquipementFilterPipe } from './equipement-filter.pipe';

describe('EquipementFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipementFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
